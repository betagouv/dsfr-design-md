[project]
name = "dsfr-design-md"
version = "0.1.0"
description = "DSFR Design System Markdown Specification"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["scripts"]
